/**
 * Перевод интерфейса. Своя реализация вместо библиотеки — не ради экономии строк, а ради одного
 * свойства: недоперевод должен быть ошибкой компиляции. Английский словарь выводится из русского
 * отображённым типом, поэтому пропущенный ключ, лишний ключ, строка вместо форм числа и
 * недостающая категория ломают сборку. Ни i18next, ни react-intl такого не дают: у них словари —
 * это JSON, а формы числа — суффиксы в строковых ключах.
 *
 * Синтаксис сообщений намеренно держится подмножеством ICU (`{имя}` и формы по категориям CLDR),
 * чтобы переезд на библиотеку когда-нибудь остался механическим.
 *
 * Локали контент-пака это не касается: интерфейс и содержимое региона — разные пространства
 * ключей (ADR 0004). Тексты плейбуков переводит `pickText`, а не `t`.
 */
export { type Locale } from '@life-os/domain';
export type { MessageKey, Params, TFunction } from './dict';
export { pluralCategories, type PluralForms } from './plural';
export { getLocale, setLocale, subscribe, resetLocaleForTests } from './store';
export { t, translate } from './t';
export { useLocale, useT, useLocaleSetting } from './react';
